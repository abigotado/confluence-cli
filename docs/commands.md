<!-- Code generated by tools/gencommands. DO NOT EDIT. -->

# Command reference

JSON mode writes one stable envelope to stdout and is the default when stdout is not a TTY. Text, raw, and help output are explicitly selected exceptions. Network commands require an explicit `--profile`; there is no active or default profile. See `contract.md` for exit codes and envelopes.

## JSON response recovery

Apply this grammar in order:

1. A v1 envelope is exactly one complete top-level JSON object followed only by
   whitespace. It requires boolean `ok` and a JSON integer `v` whose value is
   exactly `1`. Decimal, exponent, string, null, boolean, and every other
   version representation is unsupported. If `ok` is `true`, `data` must be
   present and non-null; `error` and `hint` must be absent, and their presence
   is invalid even when `null`. If `ok` is `false`, `data` must be absent;
   `error` must be a present non-null object with string `code` and `message`,
   and `hint` must be a present string. Forbidden-key presence is invalid even
   when `null`. `meta` is optional; unknown additive fields are tolerated. They
   never repair an invalid known member. A valid v1 envelope is the primary
   result; never replace it with stderr. Any malformed or unsupported envelope
   makes stdout invalid.

   Valid v1 examples:

   `{"ok":true,"v":1,"data":{},"meta":{"future":true},"future":true}`

   `{"ok":false,"v":1,"error":{"code":"PROFILE_REQUIRED","message":"profile is required","future":true},"hint":"pass --profile","future":true}`

   Invalid examples:

   - `{"ok":true,"v":2,"data":{}}`
   - `{"ok":true,"v":1.0,"data":{}}`
   - `{"ok":"true","v":1,"data":{}}`
   - `{"ok":true,"v":"1","data":{}}`
   - `{"ok":true,"v":1}`
   - `{"ok":true,"v":1,"data":null}`
   - `{"ok":true,"v":1,"data":{},"error":null}`
   - `{"ok":true,"v":1,"data":{},"hint":null}`
   - `{"ok":false,"v":1,"hint":"stop"}`
   - `{"ok":false,"v":1,"error":null,"hint":"stop"}`
   - `{"ok":false,"v":1,"error":{"code":1,"message":"failed"},"hint":"stop"}`
   - `{"ok":false,"v":1,"error":{"code":"FAILED","message":1},"hint":"stop"}`
   - `{"ok":false,"v":1,"error":{"code":"FAILED","message":"failed"},"hint":1}`
   - `{"ok":false,"v":1,"error":{"code":"FAILED","message":"failed"},"hint":"stop","data":null}`

2. Only for a `confluence-cli pages create` or `confluence-cli pages update`
   invoked with both `--confirm-intent` and `--yes`, without `--dry-run`, whose
   stdout is empty, malformed, unsupported, or otherwise fails the complete v1
   grammar above, inspect at most the first 4096 bytes of stderr. Do not use
   stderr as a contract fallback in any other case.
3. In that bounded diagnostic, only one of these exact complete,
   newline-terminated lines counts
   as the known-applied marker:

   - `error: WRITE_APPLIED_LOCAL_FAILURE: pages.create applied, but local finalization failed`
   - `error: WRITE_APPLIED_LOCAL_FAILURE: pages.update applied, but local finalization failed`

   Match from the anchored `error: WRITE_APPLIED_LOCAL_FAILURE:` at line start
   through line end, never as a substring. This exact marker means known applied
   and no retry; the write must not be repeated.
4. Otherwise, do not retry and do not claim that the write applied. Use bounded
   Confluence reads to reconcile page ID, space, title, parent, version, and
   content before asking the operator about any further mutation.

## Global flags

| Flag | Type | Description |
| --- | --- | --- |
| `--dry-run` | bool | preview a supported mutation without applying it |
| `--fields` | stringSlice | comma-separated fields to emit |
| `-o, --output` | string | output format: text, json, or raw |
| `--profile` | string | named Confluence profile (required for every network command) |
| `--timeout` | duration | abort the command after this duration |
| `-v, --verbose` | bool | write redacted request activity to stderr |
| `--yes` | bool | confirm a supported mutation |

## Commands

### `confluence-cli auth allow-spaces clear`

Remove the current profile's local page-write policy

No flags of its own.

### `confluence-cli auth allow-spaces set`

Replace the exact spaces allowed for page writes

| Flag | Type | Default | Description |
| --- | --- | --- | --- |
| `--space-id` | stringArray |  | exact numeric Confluence space ID to allow (repeatable) |

### `confluence-cli auth allow-spaces show`

Show the current profile's non-secret page-write policy

No flags of its own.

### `confluence-cli auth list`

List non-secret profile metadata without reading Keychain

No flags of its own.

### `confluence-cli auth login`

Verify and store one scoped token in macOS Keychain

| Flag | Type | Default | Description |
| --- | --- | --- | --- |
| `--capability` | string | `read` | declared credential capability: read or page-write |
| `--email` | string |  | Atlassian account email |
| `--expires-at` | string |  | optional token expiry date (YYYY-MM-DD) |
| `--site` | string |  | Confluence Cloud site, exactly https://<tenant>.atlassian.net |
| `--token-stdin` | bool |  | read one bounded scoped token from stdin without echo |

### `confluence-cli auth logout`

Delete one exact credential and its profile metadata

No flags of its own.

### `confluence-cli auth migrate-keychain`

Make one existing Keychain entry stable across CLI rebuilds

No flags of its own.

### `confluence-cli auth status`

Show local profile and Keychain presence, optionally checking Confluence

| Flag | Type | Default | Description |
| --- | --- | --- | --- |
| `--check` | bool |  | verify the three required read operations against Confluence |

### `confluence-cli contract`

Print the versioned envelope and exit-code contract

No flags of its own.

### `confluence-cli pages create`

Create one page in an explicitly allowed space

| Flag | Type | Default | Description |
| --- | --- | --- | --- |
| `--body-file` | string |  | path to one bounded storage-format body file |
| `--confirm-intent` | string |  | exact intent_sha256 from the reviewed dry-run receipt |
| `--parent-id` | string |  | optional exact numeric parent page ID; omit for a root page |
| `--representation` | string | `storage` | page body representation: storage |
| `--space-id` | string |  | exact numeric destination space ID |
| `--title` | string |  | bounded page title |

### `confluence-cli pages get`

Read one page by exact numeric ID

Usage: `confluence-cli pages get PAGE_ID`

| Flag | Type | Default | Description |
| --- | --- | --- | --- |
| `--body-format` | string | `none` | body format: none, storage, view, or atlas_doc_format |

### `confluence-cli pages list`

Read one bounded page collection

| Flag | Type | Default | Description |
| --- | --- | --- | --- |
| `--cursor` | string |  | opaque cursor from meta.next_cursor |
| `--limit` | int | `25` | maximum pages in this page (1-100) |
| `--space-id` | string |  | optional exact numeric space ID |
| `--status` | string | `current` | page status: current, archived, or draft |
| `--title` | string |  | optional exact page title filter |

### `confluence-cli pages update`

Update one current page in an explicitly allowed space

Usage: `confluence-cli pages update PAGE_ID`

| Flag | Type | Default | Description |
| --- | --- | --- | --- |
| `--body-file` | string |  | path to one bounded storage-format body file |
| `--confirm-intent` | string |  | exact intent_sha256 from the reviewed dry-run receipt |
| `--expected-version` | int | `0` | exact current positive page version |
| `--representation` | string | `storage` | page body representation: storage |
| `--space-id` | string |  | exact numeric space ID that must own the page |
| `--title` | string |  | bounded replacement page title |

### `confluence-cli search`

Run one bounded CQL content search

| Flag | Type | Default | Description |
| --- | --- | --- | --- |
| `--cql` | string |  | bounded Confluence Query Language expression |
| `--cursor` | string |  | opaque cursor from meta.next_cursor |
| `--limit` | int | `25` | maximum results in this page (1-100) |

### `confluence-cli skills install`

install the Confluence Agent Skill for Codex and/or Claude Code

| Flag | Type | Default | Description |
| --- | --- | --- | --- |
| `--dest` | string |  | explicit skills root for one provider |
| `--project-dir` | string | `.` | project directory for project scope |
| `--provider` | string |  | target provider: codex, claude, or all |
| `--scope` | string | `user` | install scope: user or project |

### `confluence-cli skills uninstall`

uninstall the Confluence Agent Skill for Codex and/or Claude Code

| Flag | Type | Default | Description |
| --- | --- | --- | --- |
| `--dest` | string |  | explicit skills root for one provider |
| `--project-dir` | string | `.` | project directory for project scope |
| `--provider` | string |  | target provider: codex, claude, or all |
| `--scope` | string | `user` | install scope: user or project |

### `confluence-cli spaces get`

Read one space by exact numeric ID

Usage: `confluence-cli spaces get SPACE_ID`

No flags of its own.

### `confluence-cli spaces list`

Read one bounded space page

| Flag | Type | Default | Description |
| --- | --- | --- | --- |
| `--cursor` | string |  | opaque cursor from meta.next_cursor |
| `--limit` | int | `25` | maximum spaces in this page (1-100) |

### `confluence-cli version`

Print the version and Go build stamp

No flags of its own.
